require("dotenv").config();
const path = require('path');

module.exports = {
  'env': process.env.NODE_ENV,
  'config': path.resolve('app', 'core', 'database', 'config.js'),
  'models-path': path.resolve('app', 'core', 'database', 'models'),
  'seeders-path': path.resolve('app', 'core', 'database', 'seeders'),
  'migrations-path': path.resolve('app', 'core', 'database', 'migrations')
};